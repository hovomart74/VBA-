11*43
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски
    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր " : Znaki(2, 2) = "հազար "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim K As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(K - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(K) = ""
                    TivArm(K - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(K - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(K - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(K - 2) = Znaki(2, 0)
                End If

            End If

            'Считаем от 1000
            If Len(Tiv) > 3 Then
                TivArm(K - 3) = Znaki(0, Val(Tiv(T - 3))) & Znaki(2, 2)
                'исключение для 1000 => "հազար"
                'чтобы вместо "հազար" не писать "մեկհազար"
                If Tiv(T - 3) = "1" Then
                    TivArm(K - 3) = Znaki(2, 2)
                End If
                'чтобы вместо "" не писать "" & "հազար"
                If Tiv(T - 3) = "0" Then
                    TivArm(K - 3) = Znaki(2, 0)
                End If
            End If

            'Считаем от 10000
            If Len(Tiv) > 4 Then
                TivArm(K - 4) = Znaki(1, Val(Tiv(T - 4)))
                If Tiv(T - 4) = "1" And Tiv(T - 3) = "0" Then
                    TivArm(K - 4) = Znaki(1, 10) & Znaki(2, 2)
                End If
            End If



        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To K
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If



        'Отправляем на вывод в ListBox1
        Return (Tiv & "=>" & VremTiv)
    End Function

    'цикл для перевода на разные языки
    Private Sub Czikl()
        For i = FromNum To ToNum
            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'отправляем числа по очереди функции ARM
                    'с последующим выводом результатов в ListBox1
                    ListBox1.Items.Add(ARM(i))
            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Czikl()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
11*53
+++++++++++++++++++++++++++++++++++++++++

Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски
    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր " : Znaki(2, 2) = "հազար "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim K As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(K - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(K) = ""
                    TivArm(K - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(K - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(K - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(K - 2) = Znaki(2, 0)
                End If

            End If






        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To K
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If



        'Отправляем на вывод в ListBox1
        Return (Tiv & "=>" & VremTiv)
    End Function

    'цикл для перевода на разные языки
    Private Sub Czikl()
        For i = FromNum To ToNum
            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'отправляем числа по очереди функции ARM
                    'с последующим выводом результатов в ListBox1
                    ListBox1.Items.Add(ARM(i))
            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Czikl()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
12*36
+++++++++++++++++++++++++++++++++++++++++

Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски
    '1111111111111111111111111111111111111111111111111111111111111111111
    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր " : Znaki(2, 2) = "հազար "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim TA As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(TA - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(TA) = ""
                    TivArm(TA - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(TA - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(TA - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(TA - 2) = Znaki(2, 0)
                End If

            End If






        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To TA
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If



        'Отправляем на вывод в ListBox1
        Return (Tiv & "=>" & VremTiv)
    End Function
    '1111111111111111111111111111111111111111111111111111111111111111111111

    'цикл для перевода на разные языки
    Private Sub Czikl()
        For i = FromNum To ToNum
            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'отправляем числа по очереди функции ARM
                    'с последующим выводом результатов в ListBox1
                    ListBox1.Items.Add(ARM(i))
            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Czikl()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
12*56
+++++++++++++++++++++++++++++++++++++++++++++

Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски

    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր " : Znaki(2, 2) = "հազար "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim TA As Integer = UBound(TivArm)





        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(TA - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(TA) = ""
                    TivArm(TA - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(TA - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(TA - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(TA - 2) = Znaki(2, 0)
                End If

            End If





        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To TA
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If



        'Отправляем на вывод в ListBox1
        Return (Tiv & "=>" & VremTiv)

    End Function


    'цикл для перевода на разные языки
    Private Sub Czikl()
        For i = FromNum To ToNum
            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'отправляем числа по очереди функции ARM
                    'с последующим выводом результатов в ListBox1
                    ListBox1.Items.Add(ARM(i))
            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Czikl()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
16*07
+++++++++++++++++++++++++++++++++

Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски

    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim TA As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(TA - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(TA) = ""
                    TivArm(TA - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(TA - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(TA - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(TA - 2) = Znaki(2, 0)
                End If

            End If

        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To TA
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If

        'Отправляем на вывод в ListBox1
        Return VremTiv

    End Function


    Private Function HazarARM(Tiv As String)
        Dim i As String
        Return (ARM(i) & "հազար" & ARM(i))
    End Function
    ''+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

    'Private Function MillionArm(Tiv As String)

    'End Function

    'Private Function MilliardArm(Tiv As String)

    'End Function

    'Private Function TrillionArm(Tiv As String)

    'End Function
    ''+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

    'цикл для перевода на разные языки
    Private Sub Czikl()
        For i = FromNum To ToNum
            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'и цифры находятся в интервале 0-999
                    If i >= 0 And i < 1000 Then

                        'отправляем числа по очереди функции ARM
                        'с последующим выводом результатов в ListBox1
                        ListBox1.Items.Add(i & " => " & ARM(i))
                    End If
                    'если цифры находятся в интервале 1000-999999
                    If i >= 1000 And i < 1000000 Then
                        'знаки цифры розделяем на группы по три символа
                        Dim Tret As String = Len(i) / 3
                        'отправляем числа по очереди функции ARM
                        'с последующим выводом результатов в ListBox1
                        ListBox1.Items.Add(i & "=>" & ARM(i))
                    End If
            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Czikl()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
16*40
++++++++++++++++++++++++++++++++++++++++++++++
Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски

    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim TA As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(TA - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(TA) = ""
                    TivArm(TA - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(TA - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(TA - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(TA - 2) = Znaki(2, 0)
                End If

            End If

        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To TA
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If

        'Отправляем на вывод в ListBox1
        Return VremTiv

    End Function


    Private Function HazarARM(Tiv As String)
        Dim i As String
        Return (ARM(i) & "հազար" & ARM(i))
    End Function
   

    'Private Function MillionArm(Tiv As String)

    'End Function

    'Private Function MilliardArm(Tiv As String)

    'End Function

    'Private Function TrillionArm(Tiv As String)

    'End Function
    

    'цикл для перевода на разные языки
    Private Sub Czikl()
        For i = FromNum To ToNum
            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'и цифры находятся в интервале 0-999
                    If i >= 0 And i < 1000 Then

                        'отправляем числа по очереди функции ARM
                        'с последующим выводом результатов в ListBox1
                        ListBox1.Items.Add(i & " => " & ARM(i))
                    End If
                    'если цифры находятся в интервале 1000-999999
                    If i >= 1000 And i < 1000000 Then
                        'знаки цифры розделяем на группы по три символа
                        Dim Tret As String = Math.Ceiling(Len(i) / 3)

                        'отправляем числа по очереди функции ARM

                        'с последующим выводом результатов в ListBox1
                        ListBox1.Items.Add(i & "=>" & ARM(i))
                    End If
            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Czikl()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
19*38
+++++++++++++++
Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 1000

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски

    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim TA As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(TA - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(TA) = ""
                    TivArm(TA - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(TA - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(TA - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(TA - 2) = Znaki(2, 0)
                End If

            End If

        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To TA
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If

        'Отправляем на вывод в ListBox1
        Return VremTiv

    End Function


    'разборка цифры в понятный формат для функции ARM
    Private Sub Razbor()
        'цикл поочерёдного вывода всех цифр из TextBox1 и TextBox2
        For i = FromNum To ToNum
            'i сохраняем ещё и в формате String
            Dim Tiv As String = i
            'максимальный индекс у вродебы массива Tiv назавём T
            Dim T As Integer = Len(Tiv) - 1

            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'знаки цифры розделяем на группы по три символа
                    Dim Tret As Integer = Math.Ceiling(Len(i) / 3)
                    'создаём массив ItogMassiv с количеством элементов Tret
                    Dim ItogMassiv(Tret + 2) As String
                    'максимальный индекс у массива ItogMassiv назавём iM
                    Dim iM As Integer = UBound(ItogMassiv)
                    'Запускаем цикл для заполнения ItogMassiv трёхсимволными блоками из цифры
                    For B = 0 To Tret
                        ItogMassiv(iM - B) = Tiv((T - 2) - (B * 3)) &
                            Tiv((T - 1) - (B * 3)) & Tiv(T - (B * 3))
                    Next
                    'создаём массив от которого будет пользоваться цикл, заполняющий ItogArm
                    Dim Miliardi() As String = {"", "հազար ", "միլիոն", "միլիարդ", "տրիլիոն"}
                    'создаём переменную ItogArm, которая в итоге отправится в ListBox1
                    Dim ItogArm As String
                    ItogArm = ""

                    For P = 0 To iM
                        ItogArm = ItogArm & ItogMassiv(P) & Miliardi(P)
                    Next

                    ListBox1.Items.Add(i & " => " & ItogArm)

            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Razbor()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
20*20
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Public Class Form1
    'тут обозначены общедоступные переменные 
    'чтобы не было длиннющих названий в кодах
    'переменная для TextBox1.Text
    Dim FromNum As Integer
    'переменная для TextBox2.Text
    Dim ToNum As Integer
    'тут устанавливаем верхний предел для ToNum
    Dim VerxPredel As Integer = 999999999

    'подпрограмма(ФУНКЦИЯ), которая считает по армянски

    Private Function ARM(Tiv As String)
        'создаём двухмерный массив для переводов на армянский
        Dim Znaki(15, 20) As String
        Znaki(0, 0) = "" : Znaki(0, 1) = "մեկ" : Znaki(0, 2) = "երկու" : Znaki(0, 3) = "երեք"
        Znaki(0, 4) = "չօրս" : Znaki(0, 5) = "հինգ" : Znaki(0, 6) = "վեց" : Znaki(0, 7) = "յոթ"
        Znaki(0, 8) = "ութ" : Znaki(0, 9) = "ինը" : Znaki(0, 10) = "զրո"
        Znaki(1, 0) = "" : Znaki(1, 1) = "տասնը" : Znaki(1, 2) = "քսան" : Znaki(1, 3) = "երեսուն"
        Znaki(1, 4) = "քարասուն" : Znaki(1, 5) = "հիսուն" : Znaki(1, 6) = "վաթսուն" : Znaki(1, 7) = "յոթանասուն"
        Znaki(1, 8) = "ութանասուն" : Znaki(1, 9) = "ինըսուն" : Znaki(1, 10) = "տաս"
        Znaki(2, 0) = "" : Znaki(2, 1) = "հարյուր "
        'Tiv нужен в формате String
        'Tiv представляем себе как массив и
        'Создаём переменную, равную максимальному индексу у элементов массива Tiv
        Dim T As Integer = Len(Tiv) - 1
        'создаём массив TivArm, количеством элементов 
        'чуть больше количества элементов Tiv (на всякий случай)
        Dim TivArm(T + 5) As String
        'Создаём переменную, равную максимальному индексу у элементов массива TivArm
        Dim TA As Integer = UBound(TivArm)

        'запускаем цикл чтобы заполнить массив TivArm переводами чисел
        'формулы привязаны к разряду знака Tiv 
        For i = 0 To T
            'немножко извращяемся, чтобы заполнять массив TivArm с конца
            TivArm(TA - i) = Znaki(i, Val(Tiv(T - i)))
            'исключение для 10 => "տաս"
            If Len(Tiv) > 1 Then
                If Tiv(T - 1) = "1" And Tiv(T) = "0" Then
                    TivArm(TA) = ""
                    TivArm(TA - 1) = Znaki(1, 10)
                End If
            End If
            'Считаем от 100
            If Len(Tiv) > 2 Then
                TivArm(TA - 2) = Znaki(0, Val(Tiv(T - 2))) & Znaki(2, 1)
                'исключение для 100 => "հարյուր"
                'чтобы вместо "հարյուր" не писать "մեկհարյուր"
                If Tiv(T - 2) = "1" Then
                    TivArm(TA - 2) = Znaki(2, 1)
                End If
                'чтобы вместо "" не писать "" & "հարյուր"
                If Tiv(T - 2) = "0" Then
                    TivArm(TA - 2) = Znaki(2, 0)
                End If

            End If

        Next
        'на основе массива TivArm создаём времменное выражение для 
        'отправки на вывод в ListBox1
        Dim VremTiv As String
        VremTiv = ""
        For v = 0 To TA
            VremTiv = VremTiv & TivArm(v)
        Next
        If Tiv = 0 Then
            VremTiv = Znaki(0, 10)
        End If

        'Отправляем на вывод в ListBox1
        Return VremTiv

    End Function


    'разборка цифры в понятный формат для функции ARM
    Private Sub Razbor()
        'цикл поочерёдного вывода всех цифр из TextBox1 и TextBox2
        For i = FromNum To ToNum
            'i сохраняем ещё и в формате String
            Dim Tiv As String = i
            'максимальный индекс у вродебы массива Tiv назавём T
            Dim T As Integer = Len(Tiv) - 1

            'проверяем какой язык выбран
            Select Case ComboBox1.SelectedItem
                Case Is = "Հայերեն"
                    'если выбрали Հայերեն
                    'знаки цифры розделяем на группы по три символа
                    Dim Tret As Integer = Math.Ceiling(Len(Tiv) / 3)
                    'создаём массив ItogMassiv с количеством элементов Tret
                    Dim ItogMassiv(Tret + 6) As String
                    'максимальный индекс у массива ItogMassiv назавём iM
                    Dim iM As Integer = UBound(ItogMassiv)
                    'Запускаем цикл для заполнения ItogMassiv трёхсимволными блоками из цифры
                    For B = 0 To Tret
                        ItogMassiv(iM - B) = Tiv((T - 2) - (B * 3)) & Tiv((T - 1) - (B * 3)) & Tiv(T - (B * 3))
                    Next
                    'создаём массив от которого будет пользоваться цикл, заполняющий ItogArm
                    Dim Miliardi() As String = {"", "հազար ", "միլիոն", "միլիարդ", "տրիլիոն"}
                    'создаём переменную ItogArm, которая в итоге отправится в ListBox1
                    Dim ItogArm As String
                    ItogArm = ""

                    For P = 0 To iM
                        ItogArm = ItogArm & ItogMassiv(P) & Miliardi(P)
                    Next

                    ListBox1.Items.Add(i & " => " & ItogArm)

            End Select

        Next
        If ToNum = VerxPredel And ComboBox1.SelectedItem = "Հայերեն" Then
            'сообщение по умолчанию, для слишком больших чисел
            ListBox1.Items.Add("Մինչև " & ToNum + 1 & " դեռ հաշվել չգիտեմ ;)")
        End If
    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        'принуждаем выбрать язык то есть, проверяем выбрали ли язык
        Select Case ComboBox1.SelectedItem
            'если не выбрали ничего,
            Case Is = ""
                'ругаем в MsgBox
                MsgBox("Ընտրի'ր լեզուն, որով հաշվելու եմ", 49, "Բարկացա'")
        End Select

        'добавляем переменные, чтобы избавиться от длиннющих названий в кодах
        FromNum = Val(TextBox1.Text)
        ToNum = Val(TextBox2.Text)

        'защита от дурака
        'в случае, если хотят принудить считать в обратном порядке то
        'меняем местами FromNum и ToNum
        If FromNum > ToNum Then
            Dim vrem As Integer
            vrem = FromNum
            FromNum = ToNum
            ToNum = vrem
        End If

        'решаем проблему с большими числами
        'если число больше, чем готовые формулы переводов
        'устанавливаем ToNum = максимально желаемое число,
        'чтобы цикол не гонять в пустую
        If ToNum > VerxPredel Then
            ToNum = VerxPredel
        End If

        'очищяем ListBox1.Items
        ListBox1.Items.Clear()

        'решаем проблему с отрицательными числами
        If FromNum < 0 Then
            FromNum = 0
            'запускаем подпрограмму ругательств по поводу отрицательных чисел
            Otricateln()
        End If

        'запускаем цикл и отправяем FromNum для начала счётчика
        Razbor()
    End Sub

    'при вводе отрицательных чисел
    Private Sub Otricateln()
        Select Case ComboBox1.SelectedItem
            'ответ на армянском
            Case Is = "Հայերեն"
                ListBox1.Items.Add("Բացասական թվեր չե'մ հաշվելու,")
                ListBox1.Items.Add("ի'նքդ կարող ես ամեն թվից առաջ ասել <<մինուս>>")
        End Select
    End Sub

End Class

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
